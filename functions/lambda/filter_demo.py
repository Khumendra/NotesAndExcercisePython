# ctrl + ?
# shift + alt + down key
# shift + alt + up key
num1 = int(input('Number1: '))
num2 = int(input('Number2: '))

# lambda param : expression
add = lambda x, y : x + y 

print(
    add(x=num1, y=num2)
)